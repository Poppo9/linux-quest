Review the current state of the project and update the three documentation files to keep them in sync with reality. Do all three in sequence.

**CLAUDE.md** (`/home/pbianco/projects/linux-quest/CLAUDE.md`)
- Update the "Lesson content summary" table if section/lesson/challenge counts changed
- Update the "index.html sections" list if sections were added, removed, or reordered
- Update architecture notes if any JS class behavior, module API, or file structure changed
- Update "Planned (not yet implemented)" if something was implemented or removed from scope
- Keep the "Lesson title convention" note accurate

**README.md** (`/home/pbianco/projects/linux-quest/README.md`)
- Update the Structure section if files were added or removed
- Update the Roadmap table statuses (✅ / 🔧 / planned) to match current implementation state

**TODO.md** (`/home/pbianco/projects/linux-quest/TODO.md`)
- Move newly completed items into the "Completato (storico recente)" section with [x]
- Add any new tasks or backlog items that emerged from the session
- Remove tasks that are no longer relevant

Read each file first, then make only the changes that are actually needed. Do not rewrite sections that are still accurate.
